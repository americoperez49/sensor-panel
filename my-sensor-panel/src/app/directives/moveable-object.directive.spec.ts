import { MoveableObjectDirective } from './moveable-object.directive';

describe('MoveableObjectDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveableObjectDirective();
    expect(directive).toBeTruthy();
  });
});
