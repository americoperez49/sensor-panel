import { FontstyleDirective } from './fontstyle.directive';

describe('FontstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new FontstyleDirective();
    expect(directive).toBeTruthy();
  });
});
